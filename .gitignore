# Go build output
/bin/
/agentdeck
*.test
*.out

# UI build output (regenerated by `make ui` / install.sh)
ui/dist/
# Embedded copy of the built UI (populated at build time from ui/dist)
internal/server/ui/dist/*
!internal/server/ui/dist/index.html

# Node
node_modules/
ui/node_modules/
mcp/node_modules/

# Local runtime / editor
.DS_Store
*.log
.gocache

.agentdeck-dev/
# usability-review harness (binaries, fixtures, node_modules, screenshots) — never commit
/.review/
/scratch-fakeacp
