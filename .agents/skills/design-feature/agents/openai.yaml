interface:
  display_name: "Design Feature"
  short_description: "Turn an idea into implementation-ready specs"
  default_prompt: "Use $design-feature to turn this idea into an implementation-ready AgentDeck feature."
policy:
  allow_implicit_invocation: false
