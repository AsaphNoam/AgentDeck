# AgentDeck MCP messaging server

Placeholder; implemented in Phase 5.

This Node package will host the MCP messaging server that lets agents drop
messages into each other's mailboxes (`~/.agentdeck/messages/{agent_id}/`) and
that backs the nudger/coordination features (F8, F11). Phase 0 ships only the
scaffold so the repo layout and `install.sh` wiring are in place.
